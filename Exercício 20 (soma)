#include <stdio.h>

int main () {
    int num;
    
    printf("Digite um número inteiro:\n");
    scanf("%d", &num);
    
    printf("Soma dos números de 1 à %d:\n", num);
    
    for (int i = 0; i <= 10; i++) {
        printf("%d + %d = %d\n", num, i, num + i);
    }
    
    return 0;
}
